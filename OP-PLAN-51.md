# Проект 51 — «Оперативный план IC Group 2026»

Группа в Bitrix24 с `GROUP_ID = 51` (`OP_GROUP_ID`). Из неё строится вкладка **«План»**
(`api/op-plan.js` → `/api/op-plan`, отдаётся из снимка `public/op-plan-cache.json`).

## Иерархия

Уровень каждой задачи зашит в её **тег** вида `ОП2026:<код>` (префикс `ОП2026:`).
По коду тега определяется уровень:

| Код в теге          | Уровень                     | Кол-во |
| ------------------- | --------------------------- | ------ |
| `I`–`IX` (римские)  | 🎯 **Цель**                 | 9      |
| `1.1` (одна точка)  | 📌 **Стратзадача**          | 27     |
| `1.1.1` (две точки) | ✅ **Тактическая задача**   | 100    |
| —                   | 🔹 **Шаг** (пункт чек-листа)| 310    |

```
Цель (I…IX)
 └─ Стратзадача (1.1)
     └─ Тактическая задача (1.1.1)   ← реальная задача Bitrix
         └─ Шаг (1.1.1.1)            ← пункт чек-листа этой задачи
```

## Что где хранится в Bitrix

- **Тактическая задача** — настоящая задача Bitrix. Поля: `RESPONSIBLE_ID` (ответственный),
  `AUDITORS` (наблюдатели), `ACCOMPLICES` (соисполнители), `DEADLINE` (крайний срок).
  Именно она рисуется карточкой в плане.
- **Шаги** — пункты чек-листа внутри тактической задачи (`task.checklistitem.getlist`).
  Заголовок раньше был `1.1.1.1 Текст · до ДД.ММ.ГГГГ · исполнитель: ФИО`.
  **Сейчас** суффиксы `· до …` и `· исполнитель: …` из заголовков убраны, а участники
  чек-листа (`MEMBERS`) удалены → по шагам исполнителей/наблюдателей больше нет.
  Поэтому аватарки берутся с самой тактической задачи (ответственный + наблюдатели).
- **Цели и стратзадачи** — тоже задачи Bitrix в группе 51, но нужны только для
  группировки (по тегу).

## Как собирается дашборд

`api/op-plan.js`:
1. Тянет все задачи группы 51, раскладывает по уровням через тег `ОП2026:`.
2. Грузит чек-листы тактических задач.
3. Резолвит `RESPONSIBLE_ID` и `AUDITORS` в имя + аватар через `user.get`.
4. Возвращает дерево `goals → children (стратзадачи) → tacts (тактические, steps[])`.

Исключаются: пользователь `id 57` и «Арайлым Ташенова».

Клиент (`drawPlan` в `public/index.html`) рисует по карточке на каждую тактическую
задачу, группируя по целям; период по умолчанию — «эта и следующая неделя».
Живой `/api/op-plan` из региона Vercel часто отдаёт 504, поэтому «План» грузится
из снимка `public/op-plan-cache.json` (регенерируется локально).
